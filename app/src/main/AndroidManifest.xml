<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.hirsonf.jobbermeister" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".LoginActivity"
            android:label="Acceuil"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterCredentialsActivity"
            android:label="Crédentiels"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".RegisterProfileActivity"
            android:label="Profil"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".RegisterCompanyActivity"
            android:label="Société"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".RegisterContactApplicantActivity"
            android:label="Contact"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".RegisterContactEmployerActivity"
            android:label="Contact"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".RegisterDescriptionActivity"
            android:label="Description"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".RegisterCriteriaActivity"
            android:label="Critères"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".BrowseOffersActivity"
            android:label="Explorer les annonces"
            android:screenOrientation="portrait">
        </activity>

    </application>

</manifest>